<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MicrosoftIMELexManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrosoftIMELexManager"
    xmlns:pages="using:MicrosoftIMELexManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="微软拼音词库管理器">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Toolbar -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,12">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
                <Button Name="OpenFileButton" Click="OpenFile_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8E5;"/>
                        <TextBlock Text="打开文件（.dat/.lex）"/>
                    </StackPanel>
                </Button>
                <Button Name="SaveButton" Click="Save_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE74E;"/>
                        <TextBlock Text="保存"/>
                    </StackPanel>
                </Button>
                <Button Name="RestoreButton" Click="Restore_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE777;"/>
                        <TextBlock Text="恢复"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content - TabView -->
        <TabView Grid.Row="1" Name="MainTabView" Padding="12,8">
            <TabViewItem Name="LexTab" Header="自定义短语" IsClosable="False">
                <Frame Name="LexFrame"/>
            </TabViewItem>
            <TabViewItem Name="IHTab" Header="输入历史" IsClosable="False">
                <Frame Name="IHFrame"/>
            </TabViewItem>
            <TabViewItem Name="UDLTab" Header="自学习词汇" IsClosable="False">
                <Frame Name="UDLFrame"/>
            </TabViewItem>
            <TabViewItem Name="ViewerTab" Header="文件查看" IsClosable="False">
                <Frame Name="ViewerFrame"/>
            </TabViewItem>
        </TabView>

        <!-- Bottom Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock Name="CurrentFileText" Text="未打开文件" VerticalAlignment="Center" Foreground="Gray"/>
                <TextBlock Name="EntryCountText" Text="" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
