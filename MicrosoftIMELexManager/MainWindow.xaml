<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MicrosoftIMELexManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrosoftIMELexManager"
    xmlns:pages="using:MicrosoftIMELexManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="微软拼音词库管理器">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Toolbar -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,12">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
                <Button Name="OpenFolderButton" Click="OpenFolder_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8E5;"/>
                        <TextBlock Text="打开文件夹"/>
                    </StackPanel>
                </Button>
                <Button Name="SaveButton" Click="Save_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE74E;"/>
                        <TextBlock Text="保存"/>
                    </StackPanel>
                </Button>
                <Button Name="RestoreButton" Click="Restore_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE777;"/>
                        <TextBlock Text="恢复"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="12,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="8">
                <StackPanel Spacing="8">
                    <TextBlock Text="词库列表" FontSize="18" FontWeight="SemiBold" Margin="8,4,8,0"/>
                    <TextBlock x:Name="LibraryListHintText" Text="打开文件夹后，这里会显示可加载的词库。" TextWrapping="WrapWholeWords" Foreground="Gray" Margin="8,0,8,8"/>
                    <ListBox x:Name="LibraryListBox" SelectionChanged="LibraryListBox_SelectionChanged" DisplayMemberPath="DisplayName"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Frame Name="LexFrame" Visibility="Collapsed"/>
                <Frame Name="IHFrame" Visibility="Collapsed"/>
                <Frame Name="UDLFrame" Visibility="Collapsed"/>
                <Frame Name="ViewerFrame" Visibility="Collapsed"/>
                <Border x:Name="EmptyContentState" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
                        <TextBlock Text="请先打开文件夹" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="选择包含词库文件的文件夹后，可在左侧切换不同词库。" Foreground="Gray" TextWrapping="WrapWholeWords" HorizontalAlignment="Center" TextAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Bottom Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock Name="CurrentFileText" Text="未打开文件" VerticalAlignment="Center" Foreground="Gray"/>
                <TextBlock Name="EntryCountText" Text="" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
