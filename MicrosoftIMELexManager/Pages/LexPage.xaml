<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MicrosoftIMELexManager.Pages.LexPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrosoftIMELexManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MicrosoftIMELexManager.Models"
    xmlns:viewmodels="using:MicrosoftIMELexManager.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <viewmodels:LexViewModel x:Key="ViewModel"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
            Margin="14,0,14,0">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Name="AddNewButton" Click="AddNew_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;"/>
                        <TextBlock Text="新增词条"/>
                    </StackPanel>
                </Button>
                <Button Name="DeleteButton" Click="Delete_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE74D;"/>
                        <TextBlock Text="删除选中"/>
                    </StackPanel>
                </Button>
                <Button Name="ImportButton" Click="Import_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B5;"/>
                        <TextBlock Text="导入"/>
                    </StackPanel>
                </Button>
                <Button Name="ExportButton" Click="Export_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8C7;"/>
                        <TextBlock Text="导出"/>
                    </StackPanel>
                </Button>
                <TextBlock Text="搜索" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBox Name="SearchBox" Width="260" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="按拼音或词语筛选"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Padding="0"
                BorderThickness="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="15,12,12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="160"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1" Text="拼音" Margin="8,4" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Column="2" Text="词语" Margin="8,4" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Column="3" Text="候选位置" Margin="8,4" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Grid>

                <ListView Grid.Row="1"
                          Name="LexListView"
                          ItemsSource="{x:Bind ViewModel.FilteredEntries, Mode=OneWay}"
                          SelectionMode="Extended"
                          HorizontalAlignment="Stretch"
                          Background="Transparent"
                          BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:LexEntry">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="44"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="160"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayIndex}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4,4,9,4" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBox Grid.Column="1" Text="{Binding Pinyin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                                <TextBox Grid.Column="2" Text="{Binding Phrase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                                <DropDownButton Grid.Column="3"
                                                Content="{Binding CandidateIndex}"
                                                Width="112"
                                                HorizontalAlignment="Left"
                                                Margin="4">
                                    <DropDownButton.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="1" Tag="1" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="2" Tag="2" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="3" Tag="3" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="4" Tag="4" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="5" Tag="5" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="6" Tag="6" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="7" Tag="7" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="8" Tag="8" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                            <MenuFlyoutItem Text="9" Tag="9" CommandParameter="{x:Bind}" Click="CandidateIndexMenuItem_Click"/>
                                        </MenuFlyout>
                                    </DropDownButton.Flyout>
                                </DropDownButton>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
