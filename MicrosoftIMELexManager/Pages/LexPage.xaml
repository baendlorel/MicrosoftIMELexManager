<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MicrosoftIMELexManager.Pages.LexPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrosoftIMELexManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MicrosoftIMELexManager.Models"
    xmlns:viewmodels="using:MicrosoftIMELexManager.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <viewmodels:LexViewModel x:Key="ViewModel"/>
    </Page.Resources>

    <Grid Margin="{ThemeResource ContentDialogPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <Button Name="AddNewButton" Click="AddNew_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;"/>
                    <TextBlock Text="新增词条"/>
                </StackPanel>
            </Button>
            <Button Name="DeleteButton" Click="Delete_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74D;"/>
                    <TextBlock Text="删除选中"/>
                </StackPanel>
            </Button>
            <Button Name="ImportButton" Click="Import_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8B5;"/>
                    <TextBlock Text="导入"/>
                </StackPanel>
            </Button>
            <Button Name="ExportButton" Click="Export_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8C7;"/>
                    <TextBlock Text="导出"/>
                </StackPanel>
            </Button>
            <TextBlock Text="搜索：" VerticalAlignment="Center" Margin="16,0,4,0"/>
            <TextBox Name="SearchBox" Width="200" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="拼音或词语"/>
        </StackPanel>

        <!-- Data Grid -->
        <ListView Grid.Row="1" Name="LexListView"
                  ItemsSource="{x:Bind ViewModel.FilteredEntries, Mode=OneWay}"
                  SelectionMode="Extended"
                  HorizontalAlignment="Stretch">
            <ListView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="拼音" Margin="8,4"/>
                    <TextBlock Grid.Column="1" Text="词语" Margin="8,4"/>
                    <TextBlock Grid.Column="2" Text="候选位置" Margin="8,4"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LexEntry">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding Pinyin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                        <TextBox Grid.Column="1" Text="{Binding Phrase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                        <NumberBox Grid.Column="2" Value="{Binding CandidateIndex, Mode=TwoWay}" Minimum="1" Maximum="9" Margin="4" SpinButtonPlacementMode="Compact"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
