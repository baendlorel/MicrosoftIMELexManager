<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MicrosoftIMELexManager.Pages.ViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrosoftIMELexManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="{ThemeResource ContentDialogPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Statistics -->
        <Grid Grid.Row="0" Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="24" HorizontalAlignment="Center">
                <StackPanel Spacing="4">
                    <TextBlock Text="文件类型" Foreground="Gray" FontSize="12"/>
                    <TextBlock Name="FileTypeText" Text="-" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="文件大小" Foreground="Gray" FontSize="12"/>
                    <TextBlock Name="FileSizeText" Text="-" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="条目数量" Foreground="Gray" FontSize="12"/>
                    <TextBlock Name="EntryCountText" Text="-" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="修改时间" Foreground="Gray" FontSize="12"/>
                    <TextBlock Name="ModifiedTimeText" Text="-" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Content List -->
        <ListView Grid.Row="1" Name="ContentView"
                  SelectionMode="None"
                  HorizontalAlignment="Stretch"
                  Margin="0,12,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="4,2"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Loading Overlay -->
        <Grid Grid.Row="1" Name="LoadingOverlay" Visibility="Collapsed" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0.85">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <ProgressRing IsActive="True" Width="48" Height="48" HorizontalAlignment="Center"/>
                <TextBlock Name="LoadingText" Text="正在加载文件..." HorizontalAlignment="Center" FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- Error State -->
        <Grid Grid.Row="1" Name="ErrorState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE893;" FontSize="64" Foreground="Red" Opacity="0.7" HorizontalAlignment="Center"/>
                <TextBlock Name="ErrorTitleText" Text="加载失败" Foreground="Red" FontWeight="SemiBold" FontSize="18" HorizontalAlignment="Center"/>
                <TextBlock Name="ErrorMessageText" Text="" Foreground="Red" FontSize="13" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="500"/>
            </StackPanel>
        </Grid>

        <!-- Empty State -->
        <Grid Grid.Row="1" Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE8E5;" FontSize="64" Foreground="Gray" Opacity="0.5"/>
                <TextBlock Text="未加载文件" Foreground="Gray" HorizontalAlignment="Center"/>
                <TextBlock Text="点击上方打开单个文件按钮以查看词库内容" Foreground="DarkGray" HorizontalAlignment="Center" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
